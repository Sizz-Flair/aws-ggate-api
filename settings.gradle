rootProject.name = 'logs'

include(
    ':infra:mysql:ggate',
    ':infra:mysql:jooq-config',
    ':infra:mysql:db-jooq',
    ':ggate-api'
)

//include 'infra:mysql:db-jooq'
findProject(':infra:mysql:db-jooq')?.name = 'db-jooq'
findProject(':infra:mysql:jooq-config')?.name = 'jooq-config'

